services:
  - type: web
    name: partegeur
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --worker-class sync -w 1 --timeout 120 partegeur:app
    plan: free
